databases:
  - name: rental_development
    ipAlloList: []

services:
  - type: web
    name: project1
    env: ruby
    buildCommand: "./bin/render-build.sh"
    startCommand: bundle exec rails s
    envVars:
      - key: RAILS_MASTER_KEY
        sync: false
      - key: DATABASE_URL
        fromDatabase:
          name: rental_development
          property: connectionString
  - type: redis
    name: redis
    ipAlloList: []
    plan: free
    maxmemoryPolicy: noeviction
